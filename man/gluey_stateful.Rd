% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gluey.R
\name{gluey_stateful}
\alias{gluey_stateful}
\title{Format text with glue syntax, maintaining state across multiple calls}
\usage{
gluey_stateful(text, ..., .envir = parent.frame(), new_env = FALSE)
}
\arguments{
\item{text}{Text template with glue-style interpolation}

\item{...}{Additional values to use for interpolation}

\item{.envir}{Environment for evaluation}

\item{new_env}{Whether to create a fresh state environment}
}
\value{
Formatted text
}
\description{
This function works like \code{gluey()} but maintains pluralization state
across multiple calls. This is primarily used for document processing
where expressions are split across multiple calls.
}
\examples{
# First expression sets up the environment and stores the quantity
gluey_stateful("Found {2} file", new_env = TRUE)

# Second expression uses the quantity from the first
gluey_stateful("{?s}")
}
